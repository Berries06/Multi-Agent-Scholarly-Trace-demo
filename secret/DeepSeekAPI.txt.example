在此处粘贴服务端托管的 DeepSeek API Key（单行，无引号无空格）。

用途：前端选择 free-deepseek 选项时，后端用此 Key 走 DeepSeek 接口。
纪律：
1. 本文件在 .gitignore 的 secret/ 规则下，绝不提交、绝不发群；
2. 文件不存在或为空时，free-deepseek 选项会返回 503（显式不可用），不会静默降级；
3. 团队成员需要免费额度时，向负责人索取 Key 后自行放到本地同一路径。
