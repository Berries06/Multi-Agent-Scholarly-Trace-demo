# 外部阻塞与推动清单（2026-08-21 深夜）

> 用途：把"卡在别人/外部"的事项写成可分配、可验收的清单。每项都有阻塞原因、最小解锁动作、验收标准与建议负责人。本清单不替代 `refine-logs/EXPERIMENT_TRACKER.md`，只收外部依赖。

## P0-1 API Key（阻塞 R017–R020、真模型三智能体对比 = 命门）

- **现状**：项目根目录无 `.env`；6 家 provider 代码就绪，规则回退可用；
- **最小解锁动作**：负责人把 4 家（deepseek / openai / anthropic / kimi 或 zhipu / qwen 任一国产替代）Key 填入项目 `.env`（Key 不进聊天框、不进源码、不进日志）；
- **验收标准**：`python scripts/run_decision_experiment.py --models "deepseek:deepseek-chat,zhipu:glm-4-flash" --max-cases 8` 冒烟跑出真实 JSON 契约且无静默回退（EXPERIMENT_PLAN R017 冒烟门）；
- **建议负责人**：模型实验成员；**时限**：8-23 前（给 390 条 L2 全量留出时间）。

## P0-2 L3 人工金标准（阻塞 R004/R005 → Block 1 全部真数字）

- **现状**：`outputs/l3-sample.csv`（28 条抽查表）已于 2026-08-20 20:41 更新；双人盲标协议与标注指南待冻结；
- **最小解锁动作**：两名标注者按 `docs/协作与运维/人工任务指南.md` 独立盲标 28 条（约 2 小时/人）→ 第三人仲裁 → 计算 kappa/alpha → 冻结 gold v1 + 哈希；
- **验收标准**：`refine-logs/EXPERIMENT_TRACKER.md` R004/R005 置 DONE，gold v1 有 paper-level 切分、哈希与类分布；
- **建议负责人**：数据负责人 + 两名标注者 + 仲裁人；**时限**：8-24 前（Block 1 的 accepted precision 全部依赖它）。

## P0-3 专利正式检索（阻塞作品书"创新性"口径的最终安全）

- **现状**：web 级标题筛选已完成（`docs/研发记录/专利查新记录_2026-08-21.md`），未做权利要求全文比对；
- **最小解锁动作**：SooPAT / CNIPA / Google Patents 上跑分类号（G06F16/36 等）+ 关键词组合检索，留存检索式快照；
- **验收标准**：命中清单 + 每条的"相邻面/是否构成冲突"一行结论，并入专利查新记录；
- **建议负责人**：任意成员（1–2 小时）；**时限**：8-26 前。

## P1 演示视频素材

- **现状**：`video_materials/研海寻踪：基于多智能体博弈推理的科研知识图谱发现系统+预审核演示视频讲稿.md` 已就绪；录制需真模型 + React 界面跑通（依赖 P0-1）；
- **最小解锁动作**：P0-1 完成后按讲稿录 10 分钟，输出到 `video_materials/`；
- **验收标准**：覆盖"画像输入→多智能体调度可视化→资源生成"闭环，≤10 分钟；
- **建议负责人**：演示主讲人 + 模型实验成员；**时限**：8-30 前。

## P1 作品书定稿

- **现状**：`submission_materials/` 预审核初稿（docx/pdf）已生成，第 7 章（评测）、第 10 章（局限）需人工定稿；EASG 口径按 `docs/研发记录/最近工作差分与C1口径修订_2026-08-21.md` §3 修订；
- **验收标准**：无"首创/首次/领先"表述；硬指标三件套标注"待测→实测"状态与数据文件路径；
- **建议负责人**：写作成员；**时限**：9-01 前。
