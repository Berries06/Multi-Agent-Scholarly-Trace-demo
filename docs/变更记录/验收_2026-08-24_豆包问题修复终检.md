# 验收_2026-08-24_豆包问题修复终检

> 依据：`docs/协作与运维/技能/验收审核skill.md`（三遍审读 + 五维打分 + 硬性判定）、`docs/协作与运维/技能/科研评审skill.md`（八视角 + 深度判定）。
> 说明：本会话禁批升级（无法跑无沙箱环境），所有测试结论均注明运行环境。

## 总判定：**FAIL**

单列"声明失实"：**"前端 tsc 0 报错"与当前仓库状态不符**——实测 `frontend\node_modules\.bin\tsc.cmd --noEmit -p frontend` exit 2，4 个真实类型错误（非环境问题）。其余条目全部通过或属沙箱环境受限（见下）。

## 条目核验表

| # | 条目 | 判定 | 证据 |
|---|---|---|---|
| 1 | P0-1 前端无 05 学术工具入口 | **PASS** | grep App.tsx `skills\|学术工具\|05` → 0 命中；frontend/src 目录无学术工具页（仅 atlas/EvidenceAtlasPage/LlmModelSelector 等） |
| 2 | P0-1 skills 后端保留、口径 PRELIMINARY | **PASS** | src/yanhai/skills/ 存在 academic_researcher.py/human_signal.py/pdf_processor.py；grep `VERIFIED` → 0 命中、`PRELIMINARY` 3 处（academic_researcher.py:249/280/473）；tests/test_skills_tools.py 实测 5/5 OK，锁定 `assertEqual("PRELIMINARY")` + `assertNotIn("VERIFIED")` |
| 3 | P0-3 secret 模板入库 + 忽略规则 | **PASS** | 提交 71b5feb 含 secret/DeepSeekAPI.txt.example（+7 行）；.gitignore:20-21 = `secret/*` + `!secret/*.example`；Test-Path 该文件 True |
| 4 | P0-3 free-deepseek 无 Key 时 503 显式报错 | **PASS** | api.py:278-280：`if not state.deepseek_key: raise _error(503, "free_provider_unavailable", "服务器免费 DeepSeek 暂不可用。", retryable=True)` |
| 5 | P0-3 real_llm_smoke 缺 Key exit 2 | **PASS（代码级）** | real_llm_smoke.py:41-47 显式 `return 2` 且提示"不做静默回退"；有 Key 路径实测复现：exit 0，provider=deepseek、model=deepseek-v4-flash、194 tokens（166+28）、1274ms——与声称"272 tokens/1.66s"同量级、属运行波动，非矛盾 |
| 6 | P1 test_atlas_data | **PASS** | 实测 4/4 OK：5 领域、合计 290 篇、分层 (30/8)(30/5)(30/6)(100/78)(100/84)、未知域 `assertRaises(KeyError)`（test_atlas_data.py:58-60，与声明"KeyError"一致） |
| 7 | P1 test_api（登录门/画像共存/运行归属/摄入同意） | **无法独立复现（环境受限，非代码失败）** | 沙箱实测 4 ERROR，traceback 定性为 setUp 建 sqlite 失败：`sqlite3.OperationalError: unable to open database file`（tempfile 落在沙箱 %TEMP%，与历史 18 项环境类同源）；四个测试名与声明逐字一致；需无沙箱环境复核（本会话禁批升级） |
| 8 | 全量回归 139/139 | **数量 PASS / 全过未复现** | 实测 `Ran 139 tests`（计数一致）；沙箱内 18 项失败/错误属 %TEMP% 环境类；"139/139 OK"需无沙箱确认 |
| 9 | 前端 tsc 0 报错 | **FAIL（声明失实）** | 实测 tsc exit 2：ProductPage.tsx:407 `sendFeedback` 未定义、:418 `searchOnline` 未定义（两者存在于 api.ts:125/129，但 ProductPage.tsx:21 的 import 清单只有 getDomains/getExtractedGraph/getProfiles/runPipeline，漏导入）；:413 `RunResult` 无 `feedback` 字段（types.ts:146 起无该字段） |
| 10 | 提交/未推送/网络原因 | **PASS** | `git rev-list --count origin/main..main` = 1（唯一未推 = aa1587e）；`git config http.https://github.com.proxy` = `http://127.0.0.1:7890`，`git ls-remote origin` 实测失败："Failed to connect to github.com port 443 via 127.0.0.1"（2113ms）；python 直连 github.com 实测 HTTP 200——"未推送、网络原因"属实（措辞微差：非 DNS 解析到 127.0.0.1，而是 git 配置走 127.0.0.1 代理，代理未运行） |

## 五维打分

| 维度 | 得分 | 理由 |
|---|---|---|
| 要求覆盖度 | 4 | 十个核验点全部有对应落实；仅 tsc 一项当前不达标 |
| 细节深度 | 5 | 503/exit 2/PRELIMINARY 均为代码级显式实现，测试锁定口径 |
| 事实准确性 | 3 | "tsc 0 报错（上轮已跑）"与当前状态不符；其余数字全部可回溯（139、5×290、git ahead=1 均实测） |
| 文笔与无 AI 痕迹 | 不适用 | 本轮交付物为代码与测试，无文案交付 |
| 可执行性 | 5 | 冒烟、Atlas、skills 测试全部可实际运行并复现 |

## 修复清单（按优先级）

1. **P0（阻断）**：`frontend/src/ProductPage.tsx:21` 的 import 补上 `sendFeedback, searchOnline`；`frontend/src/types.ts` 的 `RunResult` 增加 `feedback` 字段（按后端 /api/feedback 响应形状，至少 `feedback?: { decision?: string }`）；重跑 `tsc --noEmit -p frontend` 至 exit 0；
2. **P0（提交门）**：在无沙箱环境重跑 `python -m unittest discover -s tests` 确认 139/139（本验收环境受限无法复现，作为推送前的必做门）；
3. **P1（可选）**：test_api 的 setUp 依赖 tempfile.TemporaryDirectory 属沙箱敏感写法，可改为项目内 tmp 目录 + 显式清理，使测试在沙箱亦可跑；
4. **P2（措辞）**：推送受阻原因在报告与口述中表述为"git 配置了指向 127.0.0.1:7890 的代理且代理未运行"，比"解析到 127.0.0.1"更准确。
