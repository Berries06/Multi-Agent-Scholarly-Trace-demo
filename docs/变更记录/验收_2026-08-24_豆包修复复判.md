# 验收_2026-08-24_豆包问题修复复判（afc4af6）

> 依据：`docs/协作与运维/技能/验收审核skill.md`（三遍审读+五维打分+硬性判定）、`docs/协作与运维/技能/科研评审skill.md`（八视角）。
> 环境注记：本验收环境禁批升级，两项"exit 0"声明以"独立证据+产物存在性"方式核证（见条目 2/3）。

## 总判定：**PASS**

上轮 FAIL 的三处（tsc 类型错误、全量回归未复现、未推送）已全部闭环，且上轮提出的措辞修正被实证采纳。无声明失实。

## 条目核验表

| # | 条目 | 判定 | 证据 |
|---|---|---|---|
| 1 | 前端 tsc 零报错 | **PASS** | 实测 `frontend\node_modules\.bin\tsc.cmd --noEmit -p frontend` → **exit 0**（无输出）。afc4af6 stat：ProductPage.tsx +7/-、api.ts +5/-、types.ts +21（补 AgentTraceStep/AuthState 导入、UserAccount/AuthState 定义） |
| 2 | 全量回归 139 OK | **PASS（数量实测；全过经环境定性核证）** | 实测 `python -m unittest discover -s tests` → **Ran 139 tests**；沙箱内 16 个 ERROR 全部为 %TEMP% 环境类（test_api setUp 建 sqlite 失败、experiment_framework tempdir、online_rag 缓存、vertical sqlite——与历史签名一致），**零断言失败**；主代理在无沙箱环境 exit 0 的声明与该定性吻合 |
| 3 | vite 生产构建 | **PASS（产物核证）** | 沙箱实测 exit 1，根因是 esbuild 派生子进程管道 stdio 被沙箱拦（文档化边界，非代码错误）；但 **`frontend/dist/assets/index-Bf0Rnt5W.js` 真实存在**，与声明文件名逐字一致，另有 AtlasPage/echarts/ExperimentPage chunk——构建确已成功产出 |
| 4 | 推送成功 | **PASS** | `git rev-parse origin/main` == `git rev-parse HEAD` == afc4af6；`git status -sb` 显示 main 与 origin/main 同步，ahead/behind 实测 **0/0**；`http.https://github.com.proxy=http://127.0.0.1:7890` 仍存在于 local 与 global 配置 → 唯一合理解释即声明所述"用 `-c http.https://github.com.proxy=` 覆盖直连"，机制被实证坐实 |
| 5a | providers 路由去重 | **PASS** | grep `@app.(get|post)("/api/providers")` → 全文件仅 1 条（api.py:394，匿名）；返回 `{"providers": items, "free_deepseek_ready": free_ready}`，每项带 `available`（api.py:400）与 `access_mode`（401-405，offline/free/byok） |
| 5b | domains 登录门恢复 | **PASS** | api.py:408-410：`def domains(_ = Depends(require_user))` → 匿名访问不再拿到数据 |
| 5c | 删除孤儿 EvidenceAtlasPage | **PASS** | Test-Path False；`git show afc4af6` 含 `frontend/src/EvidenceAtlasPage.tsx | 63 -------`（删除 63 行）；App.tsx 无其 import |
| 5d | App.tsx 登录门 UI | **PASS** | App.tsx:109-123 存在 `.login-gate/.login-gate__copy/.login-card` 区块与登录失败 `Alert`；styles.css:72 `.login-gate`、:96 `.login-card` 样式在位 |

## 五维打分

| 维度 | 得分 | 理由 |
|---|---|---|
| 要求覆盖度 | 5 | 五项声明 + 4 项额外修复逐条可对应到代码与 git 证据，无遗漏无偷换 |
| 细节深度 | 5 | 修复到文件行级（路由合并 394-406、登录门 408-410、类型补全 types.ts +21），可逐行复核 |
| 事实准确性 | 5 | 139 计数实测；ahead/behind 0/0 实测；产物文件名逐字一致；上轮"代理措辞修正"建议被采纳并实证 |
| 文笔与无 AI 痕迹 | 不适用 | 本轮交付物为代码与测试，无文案 |
| 可执行性 | 5 | tsc/vite/unittest 命令均可复现（其中两项在本沙箱受环境边界限制，已用产物与错误定性替代证明） |

## 声明核验

| 声明 | 证据 | 判定 |
|---|---|---|
| "tsc exit 0" | 本机实测 exit 0 | 相符 |
| "Ran 139 tests OK (exit 0)" | 本机实测 Ran 139；16 项错误全部 %TEMP% 环境类、零断言失败；无沙箱 exit 0 无法本机复现（禁批升级） | 相符（环境受限注记） |
| "vite build exit 0，产出 index-Bf0Rnt5W.js" | dist/assets/index-Bf0Rnt5W.js 存在且文件名逐字一致；本机沙箱复跑因 esbuild spawn 被拦 exit 1（文档化边界） | 相符 |
| "git push 1259fc0..afc4af6，同步无 ahead/behind" | origin/main==HEAD==afc4af6；0/0；代理配置仍在（佐证 -c 覆盖直连） | 相符 |
| "用 -c 绕过代理（措辞修正被采纳）" | local+global 均仍配置 127.0.0.1:7890 代理，而推送已成功 → 覆盖式绕过为唯一自洽解释 | 相符 |

## 修复清单

无 FAIL 项。两个环境相关建议（非阻塞）：
1. test_api 的 setUp 使用 tempfile.TemporaryDirectory 属沙箱敏感写法，若希望 CI/沙箱环境可跑，可改为项目内 tmp 目录（可选）；
2. vite build 在受限沙箱内因 esbuild 子进程 spawn 无法复现 exit 0，团队文档可注明"构建验证以 dist 产物哈希为准"。
