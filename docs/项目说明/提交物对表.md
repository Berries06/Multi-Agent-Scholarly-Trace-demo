# 提交物对表（9-05 提交前逐项核）

> 依据赛题"作品提交形式"与评分细则。状态标注：✅ 已有 / 🟡 部分 / ❌ 缺。
> 本表是提交周的行动清单，每一项都写"评委怎么验证"。

## 1. 材料文档

| 项 | 状态 | 说明 / 评委验证方式 |
|---|---|---|
| 作品设计实现方案（作品书） | 🟡 | `scripts/build_submission_book.py` 已有生成器；内容需按新资产更新（LLM 决策、140 用例、React 界面） |
| 作品介绍 | 🟡 | 可并入作品书 |
| ≤10 分钟演示视频 | ❌ | 脚本已就绪（`docs/项目说明/演示脚本.md` v2）；录制需在 React 前端 + 真模型跑通后进行 |

## 2. 软件模块

| 项 | 状态 | 说明 |
|---|---|---|
| 源代码（开源链接） | ✅ | GitHub 仓库（main 分支持续提交） |
| 可执行程序（如有） | 🟡 | 可选；`SETUP_WEB.bat` + `RUN_WEB.bat` 已给"零命令启动"；Qt 桌面版可作加分项 |
| 部署说明 | ✅ | `docs/04_deployment.md`、`deploy/`、`frontend/README.md`、`产品Web技术栈.md` |
| 单元测试用例 | ✅ | 80+ 项（本会话新增 llm_decision/fresh_kb 等）；提交包内 `tests/` 可直接 `unittest discover` |

## 3. 测试数据

| 项 | 状态 | 说明 |
|---|---|---|
| ≥1 垂直领域知识库切片 | ✅ | 3 领域（30 篇/领域；证据卡 8+5+6） |
| ≥2 组差异化学习者学情数据 | ✅ | 3 组合成画像（含输入/中间/输出完整示例 ×9） |
| **≥50 组测试用例** | ✅ 数量 | L2 已生成 140 条（28 正 + 112 负，冻结 + SHA-256）；L3 人工核验待做（🟡） |
| 幻觉率 / 适配 / 覆盖率三指标 | 🟡 | 待 L3 锚点 + 真模型对比后在冻结集上实测（附置信区间） |

## 4. 赛题特别要求

| 项 | 状态 | 说明 |
|---|---|---|
| ≥3 个明确分工的智能体 | ✅ | 5 角色（2 前置 + 3 决策）；真模型接入待 Key |
| "分析-生成-校验-决策"闭环 | ✅ | 规则闭环可演示；模型闭环代码就绪 |
| ≥3 种资源形态 | ✅ | 导读 / 实操 / 测评（测评题现从图谱三元组动态生成） |
| 学情报告可视化（盲区/难度曲线/路径图） | ✅ | 诊断雷达图 + 难度曲线 + 学习路径 |
| 多智能体调度过程可视化 | ✅ | 思考过程时间线 + 证据图谱力导向图 |
| 基于交互反馈的动态迭代 | ✅ | 太难/合适/太易 → 难度与解释策略变化 |
| 数据合规 | ✅ | 合成画像声明；真实数据未引入 |

## 5. 提交前必做清单（按优先级）

1. **真模型三智能体对比实验**（等 Key）：4 个便宜模型 × 140 条 L2 的对比报告 → 创新 25 分的实证核心；
2. **L3 人工核验**（团队 2–3 小时）：`scripts/generate_l3_sample.py` 已出 28 条抽查表 → 三指标真实锚点；
3. **录制视频**：按演示脚本 v2，React 前端 + 真模型跑通后录；
4. **README 更新**（评委打开仓库第一眼）；
5. 打包命名：`学校—申报人—作品名称—电话.zip`，发 `602808600@qq.com`（赛题指定）。
