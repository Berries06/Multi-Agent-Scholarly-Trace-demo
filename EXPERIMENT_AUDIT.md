# P0 实验完整性审计

审计时间：2026-08-15  
工程回归结论：**PASS**  
真实实证主张结论：**WARN / 不接受**  
审查独立性：`same-family`  
验收状态：`provisional`

## 结论

当前三决策智能体架构与六组实验已通过 P0 工程回归门。v3 六次完整运行均具备可复算的原始结果、汇总、CSV、报告和验证回执；`claim_ceiling` 在配置、原始行、CSV、汇总和报告之间一致；每份 `verification.json` 的状态均为 `passed`，且其五项 SHA-256 均与当前文件匹配。

该结论只证明合成/自监督代理/模拟场景下的确定性工程行为。当前没有独立真实金标准、人工评测或外部真实测试集，因而不得据此声称真实幻觉率下降、模型准确率提升、跨领域泛化或生产吞吐性能。

## 三轮审计闭环

| 轮次 | 结论 | 发现 | 处置 |
|---|---|---|---|
| 初审 | WARN | E5 计时混入固定 24-case 决策消融；`JudgeAgent` 有无效参数；缺少独立真实 GT；确定性重放不可视作独立样本 | E5 改为仅计核心流水线；移除无效参数；补齐评估类型、主张上限和运行 provenance |
| v2 | FAIL | `claim_ceiling` 未传播到 raw/CSV/summary；没有持久化验证回执 | v3 全量传播该字段；每次运行写入 `verification.json` 和精确五文件哈希，并在回执写入前后复验 |
| v3 | PASS | 六组字段传播、回执、哈希和新鲜只读复验全部通过 | P0 工程回归门关闭 |

## 六组实验的证据边界

| 实验 | 评估类型 | 精确范围 | 可接受的最高主张 |
|---|---|---:|---|
| 01 核心消融 | `synthetic_proxy` | 24 个冻结案例 × 4 变体 × 2 次确定性重放 = 192 行 | 仅支持该候选池上的机制相对行为 |
| 02 溯源证伪 | `synthetic_proxy` | 11 个合成错误案例 × 2 变体 × 3 次重放 = 66 行 | 仅支持预编码失败模式的护栏回归 |
| 03 动态知识 | `self_supervised_proxy` | 2 个合成案例 × 3 个反馈 × 2 次重放 = 12 行 | 仅支持确定性反馈/难度回归行为 |
| 04 查询鲁棒性 | `synthetic_proxy` | 8 个编写查询 × 3 次重放 = 24 行 | 仅支持预期术语与压力主张护栏回归 |
| 05 工作负载扩展 | `simulation_only` | 2 个查询 × 1/10/50 倍重复行 × 5 次重放 = 30 行 | 仅支持本机、重复模拟行、排除决策消融后的局部计时观察 |
| 06 端到端回归 | `simulation_only` | 9 个合成案例 × 3 次重放 = 27 行 | 仅支持代码路径、schema、证据 ID、轨迹和 Idea 输出回归 |

## 已验证内容

- 六组 `run_config.json` 均记录 Git HEAD/dirty 状态、Python/平台信息、计时范围、零预热、确定性重放语义以及关键源码和输入哈希。
- E5 的计时范围为 `core_pipeline_excluding_decision_ablation`；运行时默认 API 仍保留决策消融，实验框架通过 `include_ablation=False` 排除无关工作。
- 六组 `verification.json` 各自固定验证 `run_config.json`、`raw_results.json`、`summary.json`、`cases.csv`、`REPORT.md` 五个文件。
- 验证器重新计算 raw→summary、raw→CSV、summary/config→REPORT，并校验验证回执状态和哈希。
- 当前完整回归为 76 项测试通过，分支覆盖率 82%，高于 CI 门槛 75%。

## 尚未满足的真实评估条件

进入下一阶段前，需要冻结合法可用的真实全文测试集，完成双人独立标注与冲突仲裁，记录标注者、证据片段和一致性指标，并在盲测条件下使用真实模型运行。完成这些条件前，P0 结果只能作为工程回归证据。

完整三轮审查请求、答复和元数据保存在 `.aris/traces/experiment-audit/2026-08-14_run01/`；机器可读结论见 `EXPERIMENT_AUDIT.json`。
