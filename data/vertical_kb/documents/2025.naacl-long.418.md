# GLiREL

## Method
GLiREL addresses zero-shot relation extraction.
GLiREL addresses relation extraction.
GLiREL uses an open relation schema.
GLiREL extends open-type entity recognition.

## Limitation
Zero-shot relation extraction depends on accurate entity spans.
